import * as React from 'react'

export default class HomePage extends React.Component {
    render(){
      return (<div>I was gonna do a frontend but it was the 2nd day already, check the CLI interface</div>);
    }
}